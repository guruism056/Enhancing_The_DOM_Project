* Here we create an overlay by creating a new "div" element using createElement() method.
* This element is appended to the body using the appendChild() method.
* Styles and positions are then applied to the overlay.